/* SpotNotes — private field notes on a spot (gate codes, contacts, the angle
   that worked). Store-backed textarea + the privacy caption. */
import { SpotNotes } from 'photo-scout'

/** Empty notes box with placeholder + lock caption. */
export const FieldNotes = () => <SpotNotes spotId="bayshore-boulevard" />
